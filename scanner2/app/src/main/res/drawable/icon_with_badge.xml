<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Круглый фон (увеличен) -->
    <item
        android:drawable="@drawable/circle_background"
        android:width="56dp"
    android:height="56dp"/>

    <!-- Иконка камеры (увеличена и перецентрирована) -->
    <item
        android:drawable="@drawable/camera_ic"
        android:width="30dp"
    android:height="30dp"
    android:gravity="center"/>

</layer-list>